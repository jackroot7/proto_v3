# Proto v3 - Python Dependencies
# Install with: pip install -r requirements.txt

# ── Core Framework ─────────────────────────────
Django>=5.0,<7.0          # Main web framework
Pillow>=10.0              # Image handling (product photos, receipt images)
reportlab>=4.0            # PDF generation (daily reports, export)

# ── Optional but recommended ───────────────────
whitenoise>=6.0           # Serve static files efficiently on Windows (no nginx needed)
requests>=2.28            # HTTP client (used in sync engine for cloud upload)

# ── Development only ───────────────────────────
# django-extensions>=3.0  # Uncomment for shell_plus and other dev tools
openpyxl>=3.1          # Excel file support for bulk product upload
